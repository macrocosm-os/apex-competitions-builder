The competition-builder skill now treats round variation as a design step: rounds must rotate the
conditions a solution faces — not just the instances — derived from the master seed and round number,
with the difficulty distribution held stationary, part of the condition space kept out of live rounds,
and an over-fitted reference used to prove the rotation actually selects for generalization.
