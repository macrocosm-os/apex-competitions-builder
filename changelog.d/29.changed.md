The competition-builder skill now makes adversarial scoring design a build step: designers enumerate
the unintended pathways to a high score and close them inside the scoring itself, verify with an
adversarial submission set, and keep those defenses out of comments, metadata, error text, and the
miner README — disclosed only in the private onboarding handoff.
