The competition-builder skill now recommends declaring `baseline_raw_score` as 0 (or the
lower-is-better equivalent), matching every production competition, so the first scoring submission
becomes the effective baseline. The declared floor is separated from the reference solution designers
must still write, and a non-zero floor now needs a written justification in the handoff manifest.
