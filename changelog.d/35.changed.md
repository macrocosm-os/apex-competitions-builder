The competition-builder skill now bars wall-clock time from every scored term — evaluation compute is
shared and can be throttled, so elapsed-time scores break determinism and reward resubmission fishing.
Speed, where it matters, is counted in host-independent units (steps, calls, moves, tokens,
operations), and wall clock stays confined to the deadlines and timeouts that decide admissibility.
