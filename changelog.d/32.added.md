Documented the `entrypoints.generate_round` contract: the `/data/input.json` envelope the platform
writes, the `{tasks, sandbox_data}` output it reads back, and the rule that every random choice must
derive from the master seed in `generator_args.seed` or the round stops being reproducible.
