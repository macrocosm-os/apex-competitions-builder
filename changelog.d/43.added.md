Added `kind: external` for competitions whose evaluation is delegated to a partner-hosted evaluator
over HTTPS (Platform-approved only, not self-serve): a new `external_evaluator` block (`provider`,
`base_url`, `routes`, `patch` policy, `credential_env`), the `patch` submission artifact type, and
conditional requireds so external specs carry no images while solo/duel specs are unchanged.
