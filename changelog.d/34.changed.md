The competition-builder skill now sizes evaluations against the referee and player timeouts: the
budget is the worst case a submission can force (every call stalled to the per-move deadline), not the
baseline's median, and an evaluation that cannot be made to fit must be flagged in the handoff manifest
and in a new required field on the competition-onboarding issue instead of failing as a referee
timeout on every submission.
