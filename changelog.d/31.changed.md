Documented the `result.json.metadata` conventions: the `player_stats` per-player booleans the platform reads for duel outcomes, and the optional `StandardEvalMetadata` envelope a solo referee may emit to control dashboard rendering (platform always owns `score`/`raw_score`).
