{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "fixture_solo round input",
  "description": "One round is a batch of tasks. Shaped like a real round input so $ref resolution and fixture validation are exercised.",
  "type": "object",
  "additionalProperties": false,
  "required": ["tasks"],
  "properties": {
    "tasks": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["task_name", "numbers"],
        "properties": {
          "task_name": { "type": "string" },
          "numbers": {
            "type": "array",
            "items": { "type": "number" },
            "minItems": 1
          }
        }
      }
    }
  }
}
